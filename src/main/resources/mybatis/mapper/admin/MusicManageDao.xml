<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.eco.admin.dao.IMusicManageDao">

	<select id="list" resultType="com.eco.dto.MusicVO" parameterType="com.eco.dto.Paging">
		select * from(
		select * from(
			select rownum as rn, b.* from 
			( (select * from music_view where title like '%'||#{param2}||'%' order by mseq desc) b )
		) where rn &gt;= #{param1.startNum}
		) where rn &lt;= #{param1.endNum}
	</select>
	
</mapper>