<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.eco.admin.dao.IBundleManageDao">

	<select id="list" resultType="com.eco.dto.BundleVO" parameterType="com.eco.dto.Paging">
		select * from(
		select * from(
			select rownum as rn, b.* from 
			( (select * from bundle where title like '%'||#{param2}||'%' order by bmseq desc) b )
		) where rn &gt;= #{param1.startNum}
		) where rn &lt;= #{param1.endNum}
	</select>

</mapper>